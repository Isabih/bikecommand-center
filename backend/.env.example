# MQTT broker (Mosquitto)
MQTT_HOST=192.168.1.64
MQTT_PORT=1884
# MQTT_USERNAME=
# MQTT_PASSWORD=

# Lovable Cloud (Supabase) — service role preferred so the backend can
# bypass RLS when persisting telemetry. Get it from:
# Lovable Cloud → Backend → Project Settings → API Keys → service_role
SUPABASE_URL=https://bwuzyjjahvthvglpfjhj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=replace-me
# Fallback (RLS-respected) — already works because policies are public:
# SUPABASE_PUBLISHABLE_KEY=eyJhbGci...
